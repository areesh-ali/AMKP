openapi: 3.1.0
info:
  title: AMKP API
  version: 0.0.1
  description: Agentic Multimodal Knowledge Plane — Evidence + Policy + Tenancy
paths:
  /health:
    get:
      summary: Health check
      operationId: getHealth
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                required: [ok, service]
                properties:
                  ok:
                    type: boolean
                  service:
                    type: string
