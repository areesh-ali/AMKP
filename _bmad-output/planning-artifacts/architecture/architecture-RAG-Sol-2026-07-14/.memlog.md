---
scope: AMKP initiative / Knowledge Plane platform
purpose: build substrate for independently-built epics E1-E8
altitude: initiative→features/epics
updated: 2026-07-14T06:53
---

- (decision) Working mode=Fast path; deliverables=spine+HTML eng walkthrough; audience=builders primary
- (constraint) Inherited SPEC-amkp CAP-1..8, tenancy-from-auth, Evidence-not-generate MVP, SaaS-first, fail-closed isolation
- (decision) Paradigm=hexagonal modular monolith: domain←application←adapters; packages by capability
- (decision) AD stack API=NestJS 11.1.x + Node 24 LTS + TypeScript; aligns CAP-8 TS SDK + MCP Node SDK — Prevents dual-runtime drift across epics
- (decision) AD system-of-record=PostgreSQL 16 + pgvector for metadata AND vectors MVP — Prevents split-brain tenant docs vs embeddings; graduate Qdrant only if measured
- (decision) AD jobs=BullMQ + Redis for ingest/parse/eval async — Prevents sync VLM in request path
- (decision) AD cache=Redis tenant-keyed only (tenant_id in key) — Prevents cache bleed
- (decision) AD tenancy=one pgvector collection/namespace per Tenant fail-closed — Prevents pool-filter fail-open
- (decision) AD auth=API key or JWT; Tenant resolved in auth middleware into request context; never from body — Prevents spoof
- (decision) AD retrieve contract=EvidenceEnvelope JSON schema versioned; generate out of MVP API — Prevents chat-shaped drift
- (decision) AD MCP=thin Streamable HTTP facade calling same application services as REST; Tenant from connection auth; stateless per-request — Prevents second business logic
- (decision) AD observability=OpenTelemetry traces required on retrieve/ingest/agent hops; Trace API reads stored spans/events — Prevents unobservable agentic
- (decision) AD workers own Parse Ladder; API enqueues jobs only — Prevents API process OOM on multimodal
- (decision) AD deploy=SaaS single-region container compose/k8s later; API+worker+postgres+redis — VPC deferred per SPEC
- (assumption) NestJS chosen over FastAPI for single TS runtime with SDK/MCP; Python parse workers deferred until needed
- (assumption) pgvector sufficient for MVP scale; Qdrant deferred
- (version) Node 24.18.0 LTS (nodejs.org index); NestJS @nestjs/core 11.1.28; TypeScript 7.0.2; Prisma 7.8.0; BullMQ 5.80.2; OTel sdk-node 0.220.0; MCP sdk 1.29.0; pgvector npm 0.3.0; Postgres 16 + pgvector extension
- (question) When to introduce Python worker vs keep parse in TS calling external VLMs?
- (direction) Fast path draft; HTML walkthrough for eng team alongside spine
- (event) Distilled ARCHITECTURE-SPINE.md + architecture-walkthrough.html; status draft pending finalize
